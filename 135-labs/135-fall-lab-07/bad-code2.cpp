            int                         countChar(std::string line, char c) { 
                    int total = 0; 

    for(int i = 0; i < line.length(); i++) { 
        if(line[i]              == c) { 
            total++; 
                                }
                }

                    return                      total; 
                    }